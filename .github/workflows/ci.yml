name: CI

on:
  push:
    branches: ["feature/hexagonal"]
  pull_request:
    branches: ["master"]
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build-test:
    name: Build and Test (Maven, Java 11)
    runs-on: ubuntu-latest
    permissions:
      contents: read

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up JDK 11
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '11'
          cache: 'maven'

      - name: Build (Compile)
        shell: bash
        run: |
          if [ -f mvnw ]; then
            chmod +x mvnw
            ./mvnw -B -ntp clean test-compile
          else
            mvn -B -ntp clean test-compile
          fi

      - name: Run Tests
        shell: bash
        run: |
          if [ -f mvnw ]; then
            ./mvnw -B -ntp verify
          else
            mvn -B -ntp verify
          fi